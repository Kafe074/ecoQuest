library phosphor_flutter;

// Patched for recent Flutter versions where [IconData] is a `final` class
// and can no longer be extended: every icon constant is now a plain
// [IconData], so this file intentionally defines no subclasses.
